[project]
name = "betodealmeida-pandas_semantic_layer"
version = "0.3.0"
license = "Apache-2.0"

[tool.apache_superset_extensions.build]
# Files to include in the extension build/bundle
include = [
    "src/betodealmeida/pandas_semantic_layer/**/*.py",
]
exclude = []